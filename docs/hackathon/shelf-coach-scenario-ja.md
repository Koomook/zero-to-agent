# Shelf Coach シナリオ

## 概要

Shelf Coach は、Human Manager が事前に定義した `Task List` をもとに、システムが Slack や Discord に定期的に業務依頼メッセージを送り、現場 Staff がそのスレッドに返信しながら作業し、レビューを受けるマルチモーダル運用システムです。

このシステムの要点は次のとおりです。

- Human Manager が事前に業務基準を定義する
- システムが 2 時間ごとに Staff に業務依頼メッセージを送る
- Staff は新しい投稿を作るのではなく、そのスレッドに現在の状態画像を返信する
- システムは目標状態を視覚的にガイドする
- Staff が作業後に再度画像を上げると AI が一次評価する
- 最終的に Human Manager が OK / Fail を判断する
- すべての Task が OK になると支払い可能状態になる

この流れは、料理補充、空間整理、Airbnb 運営、リテール店舗管理など、さまざまな現場オペレーションに適用できます。

## 1. Human Manager の役割

Human Manager は、まず現場で実行すべき業務基準を `Task List` として定義します。

1 つの Task List の中には複数の Task が含まれます。

各 Task は次の情報を持ちます。

- `Expected Image`: 作業が正しく完了したときの理想状態画像
- `Text Guide`: その Task をどのように実行するかを説明するテキストガイド

たとえば 1 つの Task List は次のように構成できます。

- Task 1: 料理を補充する
- Task 2: テーブルを整える
- Task 3: 値札を合わせる
- Task 4: ソファを整頓する

Human Manager は各 Task ごとに事前に写真を撮っておき、どの状態が良い状態なのかを示すテキストガイドも作成しておきます。

つまり Human Manager が作るのは単なるチェックリストではなく、

- 目標状態の視覚的基準
- 作業方法の説明
- 評価基準となる構造化された Task 定義

です。

## 2. システムによる業務依頼の開始

Staff が先にメッセージを送るのではなく、システムが先に Slack や Discord に業務依頼メッセージを送ります。

たとえば、システムは 2 時間ごとに次のようなメッセージを送れます。

`Task 1 料理補充の点検時間です。現在の状態画像をこのスレッドにアップロードしてください。`

このメッセージは特定の Task に紐づいた公式の依頼メッセージであり、Staff は別の新規メッセージを作成せず、そのメッセージのスレッドに返信するだけでよい構成です。

つまり Staff の入力方法は次のとおりです。

- システムが先に Task を指定する
- Staff はそのスレッドに現在状態の画像をアップロードする
- 必要であれば短いテキストで現在の状況も添える

するとシステムはメッセージに紐づいた Task を基準に、Human Manager が事前に定義した Task 情報をすぐに参照できます。

## 3. システムによるガイド生成

システムは Staff がスレッドに投稿した画像と、そのスレッドに紐づいた Task をもとに適切な Task を取得します。

その後、該当 Task に紐づく次の情報を使います。

- Human Manager が事前登録した Expected Image
- Human Manager が作成した Text Guide

これらをもとに、システムは現在画像と目標状態を比較し、Staff がどのように作業すべきかを分かりやすいガイドとして生成します。

このガイドは単なるテキストではなく、`Expected Image を中心にした視覚的ガイド` になります。

つまりシステムは Staff に対して実質的に次のように伝えます。

- `このように補充してください`
- `このように整えてください`
- `この状態が目標です`

そして、その結果を同じスレッドに返信として返します。

## 4. Staff の実作業とスレッド再投稿

Staff はシステムが返したガイドを見て、実際に現場で作業を行います。

たとえば、

- 料理が不足していれば補充する
- ソファが散らかっていれば整える
- テーブルが汚れていれば片づける

作業が終わったら、Staff は結果画像を同じ Slack または Discord のスレッドに再度アップロードします。

この 2 回目のアップロードは `作業完了後の状態` を示す証跡です。  
つまり 1 つの Task は、1 つのシステムメッセージとその配下のスレッド会話として整理されます。

## 5. AI による一次評価

結果画像が同じスレッドに再投稿されると、システムはその画像を見て Task が適切に実行されたかを一次評価します。

ここでシステムは、

- 目標状態にどれだけ近いか
- Task ガイドがどれだけ守られたか
- 合格とみなせるレベルか

を基準に点数を付けます。

そしてシステムは Human Manager に対して、

- この Task を `OK` にするか
- それとも `Fail` にするか

を確認するよう依頼します。

つまり AI は自動採点者ですが、最終承認者ではありません。  
最終判断権は Human Manager にあります。

## 6. Human Manager の最終確認

Human Manager はダッシュボードで各 Task の状態を確認します。

ダッシュボードには Task ごとに次の情報が表示されます。

- 元々定義された Expected Image
- Text Guide
- システムが最初に送った Task 依頼メッセージ
- Staff がスレッドに最初に投稿した Before 画像
- システムが生成したガイド
- Staff が再度投稿した After 画像
- AI が付けた点数
- 現在の状態: `OK` または `Fail`

Human Manager はこれらを見て、各 Task を最終的に承認または差し戻します。

つまりダッシュボードは単なるログ画面ではなく、`現場業務のレビュー・ボード` として機能します。

## 7. 支払い可能状態

各 Task には OK または Fail の状態が付きます。

そして 1 つの Task List 内のすべての Task が OK になると、システムはその Staff が `支払い可能状態` になったと判断します。

ここで重要なのは、実際の送金システムを作る必要はないという点です。

ハッカソンのデモでは、次のような表示ができれば十分です。

- すべての Task が承認された
- 支払い可能状態に切り替わった
- `Ready to Payout` または `Payment Eligible` の状態表示

つまり、実際にお金を送る機能ではなく、`このシステムが作業レビューと報酬トリガーまで接続できる` ことを見せるのが重要です。

## 8. 全体ユーザーシナリオの要約

全体の流れは次のとおりです。

1. Human Manager が Task List を作る
2. 各 Task に Expected Image と Text Guide を登録する
3. システムが 2 時間ごとに特定 Task の依頼メッセージを送る
4. Staff がそのスレッドに現在画像を投稿する
5. システムが該当 Task を参照し、目標状態ガイドを同じスレッドに返す
6. Staff が実際に作業したあと、結果画像を同じスレッドに再投稿する
7. AI が一次点数と評価を作る
8. Human Manager がダッシュボードで OK / Fail を決める
9. すべての Task が OK になると支払い可能状態になる

## 9. 核心価値

このシナリオの核心価値は単なる画像分析ではありません。

このシステムは次を 1 つにつなげます。

- 業務定義
- 定期的な作業依頼
- 現場ガイド
- 作業証跡
- AI 評価
- Human レビュー
- 報酬可能状態

つまり Shelf Coach は、`現場作業を画像ベースで運用し、AI と人が協力してレビューし、最終的に報酬フローまで接続するオペレーションシステム` です。
