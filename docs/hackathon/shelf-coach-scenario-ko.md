# Shelf Coach 시나리오

## 개요

Shelf Coach는 Human Manager가 미리 정의한 `Task List`를 기준으로, 시스템이 Slack이나 Discord에 주기적으로 업무 요청 메시지를 보내고 현장 Staff가 해당 스레드에 답하면서 업무를 수행하고 검수받는 멀티모달 운영 시스템이다.

이 시스템의 핵심은 다음과 같다.

- Human Manager가 미리 업무 기준을 정의한다.
- 시스템이 2시간마다 Staff에게 업무 요청 메시지를 먼저 보낸다.
- Staff는 새 메시지를 만드는 대신 해당 스레드에 현재 상태 이미지를 답글로 올린다.
- 시스템은 목표 상태를 시각적으로 가이드한다.
- Staff가 일을 마친 뒤 다시 이미지를 올리면 AI가 1차 평가한다.
- 최종적으로 Human Manager가 OK 또는 Fail을 결정한다.
- 모든 Task가 OK 되면 지급 가능 상태가 만들어진다.

이 흐름은 음식 채우기, 공간 정리, Airbnb 운영, 리테일 매장 관리 등 다양한 현장 운영 시나리오에 적용할 수 있다.

## 1. Human Manager의 역할

Human Manager는 먼저 현장에서 수행해야 할 업무 기준을 `Task List` 형태로 정의한다.

하나의 Task List 안에는 여러 개의 Task가 들어 있다.

각 Task는 다음 정보를 가진다.

- `Expected Image`: 일이 잘 수행되었을 때의 목표 상태 이미지
- `Text Guide`: 그 Task를 어떻게 수행해야 하는지에 대한 텍스트 가이드

예를 들어 하나의 Task List는 아래처럼 구성될 수 있다.

- Task 1: 음식 채우기
- Task 2: 테이블 정리
- Task 3: 가격표 맞추기
- Task 4: 소파 정돈

Human Manager는 각 Task마다 미리 사진을 찍어 두고, 어떤 상태가 좋은 상태인지에 대한 텍스트 가이드도 작성해 둔다.

즉, Human Manager가 만드는 것은 단순한 체크리스트가 아니라:

- 목표 상태의 시각적 기준
- 작업 방법에 대한 설명
- 평가의 기준이 되는 구조화된 Task 정의

이다.

## 2. 시스템의 업무 요청 시작

이제 Staff가 먼저 메시지를 올리는 것이 아니라, 시스템이 먼저 Slack이나 Discord에 업무 요청 메시지를 보낸다.

예를 들어 시스템은 2시간에 한 번씩 아래와 같은 메시지를 보낼 수 있다.

`Task 1 음식 채우기 점검 시간입니다. 현재 상태 이미지를 이 스레드에 올려주세요.`

이 메시지는 특정 Task와 연결된 공식 요청 메시지이며, Staff는 별도의 새 메시지를 작성하지 않고 그 메시지의 스레드에 댓글로 응답하면 된다.

즉, Staff의 입력 방식은 아래와 같다.

- 시스템이 먼저 Task를 지정한다.
- Staff는 해당 스레드에 현재 상태 이미지를 업로드한다.
- 필요하면 짧은 텍스트로 현재 상황을 함께 적는다.

그러면 시스템은 메시지와 연결된 Task를 기준으로 Human Manager가 미리 정의해 둔 Task 정보를 바로 찾을 수 있다.

## 3. 시스템의 가이드 생성

시스템은 Staff가 스레드에 올린 이미지와 해당 스레드에 연결된 Task를 바탕으로 적절한 Task를 조회한다.

그 다음, 해당 Task에 연결된 정보를 사용한다.

- Human Manager가 미리 등록한 Expected Image
- Human Manager가 작성한 Text Guide

이 정보를 바탕으로 시스템은 현재 이미지와 목표 상태를 비교하고, Staff가 어떻게 일을 해야 하는지 이해하기 쉬운 가이드를 만든다.

이 가이드는 단순 텍스트가 아니라 `Expected Image 중심의 시각적 가이드`가 된다.

즉, 시스템은 Staff에게 사실상 이렇게 말해주는 셈이다.

- `이렇게 채우세요`
- `이렇게 정리하세요`
- `이 상태가 목표입니다`

그리고 그 결과를 같은 스레드에 다시 답글로 달아 Staff에게 돌려준다.

## 4. Staff의 실제 작업과 스레드 재업로드

Staff는 시스템이 보내준 가이드를 보고 실제 현장에서 일을 한다.

예를 들어:

- 음식이 비어 있다면 채운다
- 소파가 어질러져 있다면 정리한다
- 테이블이 지저분하면 정돈한다

작업을 마친 뒤 Staff는 결과 이미지를 다시 찍어서 같은 Slack이나 Discord 스레드에 업로드한다.

이 두 번째 업로드는 `작업 완료 후 상태`를 보여주는 증빙 역할을 한다.  
즉, 하나의 Task는 하나의 시스템 메시지와 그 아래 스레드 대화로 정리된다.

## 5. AI의 1차 평가

결과 이미지가 같은 스레드에 다시 올라오면 시스템은 이 이미지를 보고 해당 Task가 잘 수행되었는지 1차 평가를 한다.

여기서 시스템은:

- 목표 상태에 얼마나 가까운지
- Task 가이드가 얼마나 잘 지켜졌는지
- 통과 가능한 수준인지

를 기준으로 점수를 매긴다.

그리고 시스템은 Human Manager에게:

- 이 Task를 `OK`할지
- 아니면 `Fail`할지

확인하라고 요청한다.

즉, AI는 자동 채점자이지만 최종 승인자는 아니다.  
최종 판단 권한은 Human Manager에게 있다.

## 6. Human Manager의 최종 확인

Human Manager는 대시보드에서 각 Task의 상태를 확인한다.

대시보드에는 Task별로 다음 정보가 표시된다.

- 원래 정의된 Expected Image
- Text Guide
- 시스템이 먼저 보낸 Task 요청 메시지
- Staff가 스레드에 처음 올린 Before 이미지
- 시스템이 생성한 가이드
- Staff가 다시 올린 After 이미지
- AI가 매긴 점수
- 현재 상태: `OK` 또는 `Fail`

Human Manager는 이 정보를 보고 각 Task를 최종적으로 승인하거나 반려한다.

즉, 대시보드는 단순 로그 화면이 아니라 `현장 업무 검수 보드` 역할을 한다.

## 7. 지급 가능 상태

각 Task에는 OK 또는 Fail 상태가 붙는다.

그리고 하나의 Task List 안의 모든 Task가 OK가 되면, 시스템은 해당 Staff가 `지급 가능 상태`가 되었다고 판단한다.

여기서 중요한 점은 실제 송금 시스템을 만들 필요는 없다는 것이다.

해커톤 데모에서는 다음 정도만 보여주면 충분하다.

- 모든 Task가 승인됨
- 지급 가능 상태로 전환됨
- `Ready to Payout` 또는 `Payment Eligible` 상태 표시

즉, 실제 돈을 보내는 기능이 아니라 `이 시스템이 작업 검수와 보상 트리거까지 연결될 수 있다`는 점을 보여주는 것이 핵심이다.

## 8. 전체 유저 시나리오 요약

전체 흐름은 아래와 같다.

1. Human Manager가 Task List를 만든다.
2. 각 Task에는 Expected Image와 Text Guide를 등록한다.
3. 시스템이 2시간마다 특정 Task에 대한 요청 메시지를 보낸다.
4. Staff가 해당 스레드에 현재 이미지를 올린다.
5. 시스템이 해당 Task를 찾아 목표 상태 가이드를 같은 스레드에 생성해 보낸다.
6. Staff가 실제로 일을 한 뒤 결과 이미지를 같은 스레드에 다시 업로드한다.
7. AI가 1차 점수와 평가를 만든다.
8. Human Manager가 대시보드에서 OK 또는 Fail을 결정한다.
9. 모든 Task가 OK 되면 지급 가능 상태가 된다.

## 9. 핵심 가치

이 시나리오의 핵심 가치는 단순 이미지 분석이 아니다.

이 시스템은 다음을 하나로 연결한다.

- 업무 정의
- 주기적 작업 요청
- 현장 가이드
- 작업 증빙
- AI 평가
- Human 검수
- 보상 가능 상태

즉, Shelf Coach는 `현장 작업을 이미지 기반으로 운영하고, AI와 사람이 함께 검수하며, 최종적으로 보상 흐름까지 연결하는 운영 시스템`이다.
